# CLAUDE.md

이 파일은 Claude Code가 이 Unity 프로젝트에서 작업할 때 반드시 참고해야 하는 지침입니다.

## 프로젝트 개요

**장르**: 로그라이트 협동 디펜스
**핵심 레퍼런스**: 냥코대전쟁 · 팔라독(전투) + 슬더스(로그라이트 진행) + TFT(시너지)
**한 줄 요약**: 유물을 가방에 테트리스처럼 배치하고, 시너지를 맞춰 스쿼드를 구성하는 실시간 디펜스 로그라이트

## 핵심 시스템 요약

### 맵 / 진행
- 총 3개 스테이지, 스테이지마다 전투/정예전투/보스/휴식/상인/이벤트 노드로 구성된 맵이 매 런(run)마다 랜덤 생성
- 슬더스처럼 분기형 경로, 유저가 진행 경로 선택
- 각 스테이지는 보스 처치로 종료

### 전투
- 냥코대전쟁/팔라독식 실시간 레인 디펜스
- 초당 재화 자동 획득 → 재화로 유닛 소환 → 유닛 자동 전진 및 교전
- 스페이스바로 특수공격 사용 (특정 유물 획득 또는 특정 시너지 활성화 시 해금)

### 유닛
- 등급 1~5성. 스테이지별 등장 등급 범위: Stage1 1~3성 / Stage2 2~4성 / Stage3 3~5성
- 스쿼드 슬롯: 시작 3개 → 최대 10개까지 확장
- 유닛마다 특성(예: '혈마' = '무림인'+'갈증') 보유, TFT처럼 시너지 조합에 따라 전투 효과 부여

### 가방(인벤토리) — 핵심 차별점
- 시작 크기 3×3, 테트리스처럼 유물을 모양대로 배치 (슬더스처럼 자동 저장 아님)
- 유물 형태 예시: 1×1, 2×1(막대), 2×2(사각), ㄱ자(3칸) 등
- 유물은 90도 단위 회전 가능
- 가로/세로 확장을 통해 최대 4×4까지 확장 가능

### 메타 성장
- 런 종료 시 로그라이트 포인트 지급
- 포인트로 가방 칸 확장, 초당 재화 획득 효율 증가 등 영구 강화 항목에 투자

---

## 작업 규칙 (필독)

### 1. 임의 수정 금지 — 반드시 보고할 것
- 기존 코드/씬/프리팹을 요청 범위 밖에서 임의로 수정하지 말 것.
- **부득이하게 기존 로직이나 구조를 수정해야 하는 경우, 반드시 다음을 명시할 것:**
  - 어떤 파일의 어떤 부분을 수정했는지
  - 왜 그렇게 수정했는지 (기존 방식으로는 안 되는 이유 포함)
  - 수정으로 인해 영향받을 수 있는 다른 코드/씬이 있는지
- 기존 public 메서드/프로퍼티의 시그니처를 바꾸거나 삭제해야 한다면, 먼저 이유를 설명하고 확인을 받을 것.

### 2. 임의 추가 금지
- 요청하지 않은 기능, 시스템, 최적화를 먼저 추가하지 말 것. 필요하다고 판단되면 제안만 하고 실행은 확인 후 진행.
- 새로운 패키지/에셋/외부 라이브러리 설치가 필요하면 설치 전에 먼저 알릴 것.

### 3. Unity 프로젝트 관례
- **.meta 파일을 직접 수동으로 편집하거나 삭제하지 말 것** (Unity가 관리하는 파일).
- 씬(.unity)이나 프리팹(.prefab) 파일은 가능하면 Unity 에디터를 통해 수정하고, 직접 텍스트 편집이 필요하면 참조(guid) 깨짐에 특히 주의할 것.
- `Update()`에서 `GameObject.Find`, `FindObjectOfType` 같은 무거운 탐색 호출을 피할 것 — 캐싱하거나 이벤트/참조 방식 사용.
- 자주 생성/파괴되는 오브젝트(투사체, 이펙트 등)는 Object Pooling 적용을 우선 고려할 것.
- 데이터 정의(유닛 스탯, 유물 정보, 시너지 테이블 등)는 하드코딩 대신 ScriptableObject 기반으로 설계할 것.
- MonoBehaviour 생명주기(Awake/OnEnable/Start/Update 등) 순서를 지키고, 초기화 순서에 의존하는 로직은 명확히 주석으로 남길 것.

### 4. 코드 스타일
- 클래스/메서드: PascalCase, 지역 변수/매개변수: camelCase.
- private 필드는 `_camelCase` 또는 프로젝트 기존 컨벤션을 따를 것 (기존 코드 스타일이 있다면 그것을 우선).
- 매직 넘버 대신 상수(const)나 SerializeField로 노출된 값을 사용할 것.
- 주석은 "무엇을 하는지"보다 "왜 이렇게 했는지" 위주로 작성.

### 5. 작업 후 확인
- 코드를 수정/추가한 뒤에는 컴파일 에러 여부를 먼저 확인할 것.
- 변경 사항이 기존 씬/프리팹 참조에 영향을 줄 수 있으면 반드시 알릴 것.
- 큰 리팩터링은 한 번에 하지 말고 단계별로 나누어 진행하고, 각 단계마다 무엇을 바꿨는지 보고할 것.
- 커밋은 요청받았을 때만 수행하고, 임의로 git add/commit/push하지 말 것.
